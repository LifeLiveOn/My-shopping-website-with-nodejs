{{!-- <h1>Products</h1>
<form class="row g-3" method="post" action="/reports">
  <div class="col-auto">
    <label for="inputPassword2" class="visually-hidden">Password</label>
    <input type="password" name="password" class="form-control" id="inputPassword2" placeholder="Password">
  </div>
  <div class="col-auto">
    <button type="submit" class="btn btn-primary mb-3">Confirm identity</button>
  </div>
</form> --}}

<div class="container">
  <div class="row">
    {{#each list_of_products }}
    <div class="col-4">
      <h1>{{this.name}}</h1>
      <h1>{{this.price}}</h1>
      <img src="{{this.images}}">
    </div>
    {{/each}}
  </div>
</div>